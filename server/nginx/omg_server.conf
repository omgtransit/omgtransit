server {
    listen 80 default_server;
    listen [::]:80 default_server ipv6only=on;

    server_name omgtransit.com;

    root /home/app/server/public;

    # Passenger
    passenger_enabled on;
    passenger_user app;
    passenger_ruby /usr/bin/ruby1.9.1;
    
}